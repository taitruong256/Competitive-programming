#include<stdio.h>

int main()
{
    int t;
    t=1; scanf("%d", &t);
    while (t--)
    {
        long long u, v;
        long long i=1;
        long long x, y;
        while (u)
    }
    return 0;
}
